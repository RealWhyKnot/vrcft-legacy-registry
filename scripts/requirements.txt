PyNaCl==1.5.0
# Optional: pure-Python .NET assembly parser used by import-upstream.py to
# auto-detect the ExtTrackingModule subclass inside a vendor DLL. If absent,
# the import script still works but requires the operator to pass
# --upstream-type explicitly.
dnfile==0.15.1
