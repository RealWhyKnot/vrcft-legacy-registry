# Pure-Python .NET assembly parser. import-upstream.py uses it to auto-detect
# the ExtTrackingModule subclass inside a vendor DLL and to filter native DLLs
# (those without CLR metadata) when several candidates remain after the
# name-pattern exclude. Without it, an upstream zip that ships native helpers
# alongside the managed assembly needs the operator to disambiguate with
# --upstream-assembly.
dnfile==0.15.1
